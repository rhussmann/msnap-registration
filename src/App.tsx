import React from "react";
import Button from "@material-ui/core/Button";

const App: React.FC = () => {
  return (
    <Button variant="contained" color="primary">
      Get on board the fix it bus
    </Button>
  );
};

export default App;
